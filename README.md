# 🥦 FreshMart — Интернет-магазин свежих продуктов

Полноценный React-магазин без бэкенда. 96 товаров, 9 категорий.

## Запуск

**Просто откройте `index.html` в браузере — и всё работает!**

Никаких `npm install`, никакого Node.js, никакого сервера.

Если браузер блокирует локальные файлы (редко), используйте:

```bash
# Python (есть на любом Mac/Linux)
cd freshmart-react
python3 -m http.server 8080
# Открыть: http://localhost:8080

# Или npx если есть Node.js
npx serve .
```

## Функционал

- ✅ **96 товаров** в 9 категориях (фрукты, овощи, ягоды, напитки, молочные, выпечка, орехи, зелень, экзотика)
- ✅ **Главная** — баннер с анимацией, статистика, популярные, Premium-товары
- ✅ **Каталог** — фильтры по категории, цене, сортировка (6 режимов), поиск, вид сеткой/списком
- ✅ **Корзина** — боковая панель, изменение количества, итоговая сумма, кнопка оформления
- ✅ **Избранное** — сохраняется в localStorage
- ✅ **LocalStorage** — корзина и избранное живут после перезагрузки
- ✅ **Анимация "полёт в корзину"** при добавлении товара
- ✅ **Toast-уведомления** при действиях
- ✅ **Рекомендации** с кнопкой обновления
- ✅ **Адаптивный дизайн** — мобильный и десктоп
- ✅ **Модальное окно** после оформления заказа

## Структура

```
freshmart-react/
├── index.html     — HTML-оболочка с React CDN
└── src/
    ├── data.js    — данные (96 товаров, категории)
    └── App.jsx    — всё приложение (компоненты + стили)
```
